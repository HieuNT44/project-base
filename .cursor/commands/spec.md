# Spec — cập nhật notebook catalog

Invoke **`spec-driven-development`** skill.

1. Read [.notebook/README.md](../../.notebook/README.md) and catalogs `01`–`03`
2. Cross-check request against scope, function list, NFR
3. Update `.notebook/01_business-requirements/`, `02_function-list/`, `03_non-functional-requirements/` when needed
4. Hand off to `/plan` or `/define` for backlog tasks

Do not create `SPEC.md` at repo root — notebook is the source of truth.
