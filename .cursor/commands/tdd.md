# TDD — viết test RED

Invoke **`test-driven-development`** skill.

Prerequisites: `design/{id}.md` (M+ scope) or `ready/{id}.md` with §6 TTD.

1. Detail test cases on `ready/{id}.md` §6 if not already done
2. Write failing tests in `test/` (or project test layout) — **RED only**
3. Do not implement production code yet — hand off to `/build`

Reference: [.notebook/00_references/testing-patterns.md](../../.notebook/00_references/testing-patterns.md)
