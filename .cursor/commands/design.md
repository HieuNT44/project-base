# Design — handoff Code

Invoke **`design-workflow`** skill.

Prerequisites: `.backlogs/{id}/ready/{id}.md` exists.

1. Propose or update docs in `.notebook/06_design/` (BD/DD as needed)
2. Write `.backlogs/{id}/design/{id}.md` — input for TDD and Develop
3. Scope M+ **requires** design before code

Read [.workflow-sldc/04_WORKFLOW_DESIGN.md](../../.workflow-sldc/04_WORKFLOW_DESIGN.md).
